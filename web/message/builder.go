package message

import (
	"hermes/channel/message"
	"hermes/web/domain"
)

func buildSimpleMessage(template *domain.Template, message *domain.SimpleMessage) (*message.Message, error) {
	return nil, nil
}
